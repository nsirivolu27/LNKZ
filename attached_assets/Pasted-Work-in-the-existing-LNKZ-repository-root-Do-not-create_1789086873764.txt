Work in the existing LNKZ repository root. Do not create a parallel app or replace the current architecture.

Goal: make the repository match the LNKZ MCP Server project described on my resume while keeping every claim verifiable from the codebase.

First inspect README.md, ARCHITECTURE.md, CONTRIBUTING.md, MCP.md, DEPLOY.md, package.json, apps/mobile, src/lnkz, infra, scripts, tests, and .github/workflows. Preserve existing public contracts, tool names, resource URIs, environment variables, authentication guards, and user changes.

Implement and verify the following:

1. MCP server
- Confirm the server exposes the documented 25 MCP tools, 5 resources, and 4 prompts.
- Test the MCP surface with the official MCP SDK.
- Verify conversation import, search, analysis, context packets, graph tools, secure handoffs, revocation, and publishing preparation.
- Do not claim tools that are not actually registered.

2. Mobile application
- Finish the Expo mobile client in apps/mobile.
- Connect the UI to the canonical REST backend with a typed client.
- Support authentication, live stats, conversation listing/search, conversation detail, imports, context packets, handoff creation, handoff revocation, connector status, and disconnect.
- Store API credentials securely on native devices and use browser storage only for web preview.
- Keep the UI mobile-first and preserve the current LNKZ visual design.
- Verify the mobile web export and fix occupied Metro-port issues if needed.

3. Backend and security
- Verify Bearer API-key authentication and workspace isolation.
- Add correct CORS and OPTIONS preflight handling for the Replit/Expo preview origin.
- Never log API keys, handoff tokens, database passwords, or MCP context secrets.
- Reject invalid hosts and origins without weakening production validation.

4. Docker and GitHub Actions
- Verify the Docker image builds with Node 22 and the pinned pnpm version.
- Run as the non-root lnkz user.
- Include health/readiness checks and the RDS CA certificate for verified PostgreSQL TLS.
- Add CI checks for typecheck, tests, production build, mobile export, MCP verification, HTTP smoke tests, container startup, and AWS CDK synthesis.
- Do not commit generated builds, databases, logs, credentials, or local environment files.

5. AWS infrastructure
- Verify the CDK stack synthesizes without deploying resources.
- Keep ECR, image scanning, KMS encryption, private PostgreSQL RDS, App Runner, VPC networking, IAM roles, Secrets Manager, and CloudWatch logging.
- Keep App Runner disabled by default until an image exists.
- Use a separate non-owner runtime database role with least-privilege grants and RLS.
- Ensure the runtime cannot read migration credentials or write to unrelated S3/KMS resources.
- Add CDK assertions for these properties.
- Do not run cdk deploy.

6. PostgreSQL
- Ensure incomplete DATABASE_HOST/DATABASE_SECRET_JSON configuration fails clearly instead of silently falling back to SQLite.
- Verify migrations, schema version checks, workspace RLS, handoffs, search, and cleanup with a disposable PostgreSQL service.
- Keep SQLite as the local default only when no PostgreSQL configuration is supplied.

Use these commands after implementation:

corepack pnpm install --frozen-lockfile
corepack pnpm typecheck
corepack pnpm test
corepack pnpm build
corepack pnpm build:mobile
corepack pnpm verify:web
corepack pnpm verify:http
corepack pnpm verify:mobile
corepack pnpm infra:synth

For Replit configuration, document these values without hardcoding secrets:

Backend:
LNKZ_API_KEY
DATABASE_SECRET_JSON
DATABASE_HOST
DATABASE_PORT
DATABASE_NAME
DATABASE_SSL=true
LNKZ_POSTGRES_WORKSPACE_ID
ALLOWED_HOSTS=<exact backend hostname>
ALLOWED_ORIGINS=<exact Replit/Expo browser origin>
LNKZ_PUBLIC_BASE_URL=<public backend URL>

Mobile:
EXPO_PUBLIC_LNKZ_API_URL=<public backend URL>

Never place LNKZ_API_KEY in EXPO_PUBLIC_* variables. Do not invent deployment results or performance metrics. At the end, report the files changed, commands that passed, any skipped checks, and any remaining work.