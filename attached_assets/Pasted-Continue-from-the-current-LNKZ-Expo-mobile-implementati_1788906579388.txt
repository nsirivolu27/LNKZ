Continue from the current LNKZ Expo mobile implementation. The goal is to make the attached design fully functional on an actual mobile app, not just visually similar in the design preview.

Use the attached LNKZ Mobile and Field Handoff designs as the UI source of truth. Inspect the existing Expo app, LNKZ backend, API client, navigation, tests, and Metro configuration before editing.

Complete the following end-to-end flows using the real LNKZ backend:

1. Home screen
   - Load live stats from GET /api/stats.
   - Load connector status from GET /api/connectors.
   - Show loading, error, retry, and empty states.
   - Never display hard-coded production values.

2. Conversation library
   - Load GET /api/conversations.
   - Support search, refresh, and selecting a conversation.
   - Show a useful empty state when no conversations exist.

3. Import
   - Paste a real transcript.
   - Call POST /api/conversations/import.
   - Show the backend preview, warnings, and saved result.
   - Refresh the library after a successful import.

4. Conversation detail
   - Load GET /api/conversations/:id.
   - Display messages, decisions, open questions, action items, facts, and topics.
   - Add working actions for context packets and handoffs.

5. Field Handoff design
   - Match the attached handoff layout.
   - Create handoffs through POST /api/conversations/:id/handoffs.
   - Display the generated link only after success.
   - Support expiration, usage limit, audience, note, and redaction.
   - Add revoke functionality through the existing backend route.
   - Treat handoff links as bearer secrets and never log them.

6. Settings
   - Reuse the securely stored API key when retesting.
   - Allow server URL changes.
   - Show connection status and connector readiness.
   - Keep credentials out of public Expo variables and source code.

Fix the Replit preview problem instead of accepting the external “Invalid Host” page as a successful result. Determine whether it comes from the Replit proxy, Expo web preview, or the LNKZ backend. Configure the exact Replit preview hostname through the appropriate environment variables and proxy settings:

- ALLOWED_HOSTS
- ALLOWED_ORIGINS
- LNKZ_PUBLIC_BASE_URL

Do not disable host validation or use unrestricted wildcard access. The app must work in both:
- Replit browser preview
- Expo Go or a real mobile device

Implement Task #54 as part of this work: make Metro choose or receive an available port automatically, and ensure concurrent design/mockup builds do not break the mobile workflow.

Use real API data only. Remove placeholder screens and fake success states. Preserve the existing LNKZ REST contracts, authentication, storage, MCP behavior, and security protections.

Acceptance criteria:

- The attached design is implemented as actual Expo screens and navigation.
- A user can configure the server, connect, import a conversation, open it, build a packet, create a handoff, and revoke it.
- The same API client works in Replit preview and Expo Go.
- The browser preview reaches the app instead of showing Invalid Host.
- Metro handles an occupied port automatically.
- Typecheck passes.
- All mobile tests pass.
- Static Expo build passes.
- Add or update integration tests for the real stats, connectors, import, packet, and handoff flows.
- Do not report completion until you manually verify the primary flow in the Expo preview or on-device simulator.